CREATE TABLE IF NOT EXISTS public.codex (id UUID PRIMARY KEY DEFAULT uuid_generate_v4(), cycle INTEGER NOT NULL, content TEXT NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc'::text, NOW()) NOT NULL);
